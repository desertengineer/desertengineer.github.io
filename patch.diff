*** Begin Patch
*** Update File: .github/workflows/MagicBlockBlitz2026_Packages.yml
@@
-      - name: Locate and Upload APK
-        run: |
-          mkdir -p final_output
-          APK_PATH=$(find android/app/build/outputs -type f -name "*.apk" | head -n 1 || true)
-          if [ -n "$APK_PATH" ]; then
-            cp "$APK_PATH" final_output/MagicBlockBlitz2026-Final.apk
-          else
-            echo "No APK found"
-            exit 1
-          fi
-      - name: Upload Verified APK
-        uses: actions/upload-artifact@v4.6.1
-        with:
-          name: MagicBlockBlitz2026-Android-Release
-          path: final_output/MagicBlockBlitz2026-Final.apk
+      - name: Locate and Upload APK/AAB
+        run: |
+          mkdir -p final_output
+          # Find either an APK or AAB (bundle) produced by the Android build
+          APK_AAB_PATH=$(find android/app/build/outputs -type f \( -name "*.apk" -o -name "*.aab" \) | head -n 1 || true)
+          if [ -n "$APK_AAB_PATH" ]; then
+            echo "Found artifact: $APK_AAB_PATH"
+            EXT="${APK_AAB_PATH##*.}"
+            cp "$APK_AAB_PATH" final_output/MagicBlockBlitz2026-Final.$EXT
+            ls -lah final_output || true
+          else
+            echo "No APK or AAB found in android/app/build/outputs"
+            find android/app/build/outputs -type f -print || true
+            exit 1
+          fi
+      - name: Upload Verified APK/AAB
+        uses: actions/upload-artifact@v4.6.1
+        with:
+          name: MagicBlockBlitz2026-Android-Release
+          path: final_output/MagicBlockBlitz2026-Final.*
*** End Patch
*** Begin Patch
*** Update File: Apps/EnBlocks2026/capacitor.config.json
@@
-{
-  "appId": "com.bekeirataapps.magicblitzblitz2026",
-  "appName": "Magic Block Blitz 2026",
-  "webDir": "www",
-  "ios": {
-    "scheme": "App"
-  },
-  "android": {
-    "appName": "Magic Block Blitz 2026"
-  }
-}
+{
+  "appId": "com.bekeiratapps.magicblitzblitz2026",
+  "appName": "Magic Block Blitz 2026",
+  "webDir": "www",
+  "ios": {
+    "scheme": "App"
+  },
+  "android": {
+    "appName": "Magic Block Blitz 2026"
+  }
+}
*** End Patch